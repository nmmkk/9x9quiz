# 2026-02-14 M2 QA Report

## Build and Reference

* Baseline commit: `a3620b8` (`docs: record M2-06 QA pass and status sync`)
* Ticket under validation: `M2-06`

## Environment

* OS: Windows (`win32`)
* Node.js: `v24.13.1`
* npm: `11.8.0`
* Runtime validation: local Vite dev server
* Manual browser verification: user confirmed post-fix checks on PC and iOS in-session on 2026-02-14

## Executed Scenarios

| ID | Scenario | Result | Evidence |
| --- | --- | --- | --- |
| QA-01 | Unit and regression tests pass | Pass | `npm run test` -> 5 files, 19 tests passed |
| QA-02 | Production build succeeds | Pass | `npm run build` succeeded |
| QA-03 | App runtime boots locally | Pass | `npm run dev -- --host 127.0.0.1 --port 4183 --strictPort` booted |
| QA-04 | Locale storage read/write/default behavior | Pass | `src/shared/i18n/__tests__/localeStorage.test.ts` |
| QA-05 | Catalog key parity and fallback behavior | Pass | `src/shared/i18n/__tests__/catalog.test.ts` |
| QA-06 | Existing quiz and high-score regressions remain green | Pass | `src/features/quiz/domain/__tests__/draw.test.ts`, `src/features/quiz/domain/__tests__/scoring.test.ts`, `src/shared/storage/__tests__/highScoreStorage.test.ts` |
| QA-07 | Default document language and runtime lang-sync wiring are present | Pass | `index.html` (`<html lang="ja-JP">`), `src/shared/i18n/I18nProvider.tsx` (`document.documentElement.lang = locale`) |
| QA-08 | Full manual localized flow (Title -> Mode -> Quiz -> Result) | Pass | User confirmation in-session after follow-up fixes (2026-02-14) |
| QA-09 | Manual visual verification at ~360x640 and desktop widths | Pass | User confirmed post-fix size/layout are acceptable on PC and iOS |

## Feedback-driven Follow-up (2026-02-14)

* Resolved in follow-up code updates:
  * Removed standalone eyebrow labels (`タイトル`, `モード`, `クイズ`) from affected screens.
  * Renamed app title to `くくクイズ`.
  * Reduced question-count options to `10` and `20`.
  * Updated score terminology to `とくてん` / `さいこうとくてん`.
  * Switched quiz progress heading to `{current}もんめ`.
  * Rendered result status only when a new high score is achieved.
  * Emphasized numeric best score value only (no bold for `まだきろくなし`).
  * Confirmed post-fix behavior manually on PC and iOS.

## Acceptance Criteria Coverage (M2-06)

* Completed in this run:
  * Automated validations (`test`, `build`) are green.
  * Dev runtime boot is confirmed.
  * Locale persistence and catalog fallback coverage are validated by tests.
  * QA evidence is recorded and status docs are synchronized.

## Open Issues

* No automated test/build failures.
* No known blocking code defects from this run.
* No blocking issues found.

## Status Recommendation

* Ticket `M2-06`: `Completed`
* Milestone `M2`: `Completed`
